A small supermarket POS project demo desmonstrating data mining through KMeans clustering and FP-Growth algorithm using scikit-learn. Pygame was used for the UI. 
To execute create a python virtual environment, then type in the command line:
pip install requirements.txt
python main.py
